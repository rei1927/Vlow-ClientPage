#!/usr/bin/expect -f
set timeout 60
spawn ssh -o StrictHostKeyChecking=no root@pve.dayamedialangit.co.id
expect "*?assword:*"
send "Alamatgue123\r"
expect "# "
send "pct exec 100 -- bash -c 'docker logs --tail 200 $(docker ps -q --filter name=waha)'\r"
expect "# "
send "exit\r"
